#include "main.h"
#include <stdio.h>

/**
 * _isdigit - print through 0 to 9
 * @c: input to use
 * Return: 1 if digit, 0 otherwise
 */

int _isdigit(int c)
{
	if (c >= 0; c <= 9)
	{
		return (1);
	}
	else
		return (0);
}
