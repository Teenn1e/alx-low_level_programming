#include <stdio.h>

/**
 * main - entry point of program
 * Description: using the main function
 * Return: Always zero after program runs
 */
int main(void)
{
char l;
for (l = 'a'; l <= 'z'; ++l)
{
	putchar(l);
}
	putchar('\n');
return (0);
}
