#include <stdlib.h>
#include <stdio.h>
#include <time.h>

/**
 * Description: main function prints the alphabet in lowercase
 * main - entry point of program
 * Return: 0 after program runs
 */
int main(void)
{
char l; for (l = 'a', l <= 'z', ++l)
	putchar(l);
return (0); }	
